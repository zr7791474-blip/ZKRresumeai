# ZKR Resume AI — Demo build
#
# This is a UI/demo project. There is no database, authentication, or
# external API required to run it. The variables below are optional and
# only affect metadata (page titles, Open Graph tags, sitemap URLs).

NEXT_PUBLIC_APP_NAME="ZKR Resume AI"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
